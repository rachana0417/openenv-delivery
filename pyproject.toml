[project]
name = "openenv-delivery"
version = "0.1.0"
description = "OpenEnv Delivery Environment"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"